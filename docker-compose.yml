version: '3'
services:
  server:
    image: woss/remote-pgp-server
    build: 
      target: main
      context: .
      dockerfile: ./devops/prod.Dockerfile
    volumes:
      - ./keys:/keys
    ports:
      - 31111:3000
