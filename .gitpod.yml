image:
  file: ./devops/.gitpod.Dockerfile
tasks:
  - init: npm install
    command: npm install
# Ports to expose on workspace startup
ports:
  - port: 3000
    visibility: public
vscode:
  extensions:
    - PKief.material-icon-theme